task_id: L1_env_info
title: 环境信息
level: L1
skill_expected: base
query: "报告设备 {device} 的用户、主机名、系统版本"
device_var: "{device}"
repeat: null
tools_required:
  - tool: shell
    type: mcp
    required: true
    note: ""
expected_answer:
  result: "返回设备的用户、主机名、系统版本的真实值（占用设备后 shell 查 whoami/hostname/ver）"
  process: []
accept_criteria:
  sr_threshold: 0.8
  n_min: 20
  veto: false
success_condition:
  type: all
  rules:
    - type: any
      rules:
        - type: all
          rules:
            - {type: tool_called, tool: occupy_device}
            - {type: tool_called, tool: shell}
        - {type: tool_called, any_of: [Bash, Read, Glob]}
    - {type: llm_judge}
prep: ""
version: "1"
enabled: true
note: ""
