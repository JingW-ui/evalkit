task_id: L3_uu_take_code_uninstalled
title: uu 取号（未安装）
level: L3
skill_expected: uu_remote
query: "在设备 {device} 上配置 UU 取号验证码 163a163a 并写 DK 备注"
device_var: "{device}"
repeat: null
tools_required:
  - tool: uu-remote-auto
    type: skill
    required: true
    note: "加载 uu-remote-auto skill（含安装流程）走取号"
  - tool: occupy_device
    type: mcp
    required: true
    note: ""
  - tool: shell
    type: mcp
    required: true
    note: ""
  - tool: dk_note_updater
    type: local_script
    required: true
    note: "写 DK 备注"
expected_answer:
  result: "先安装 UU 再取号：取得真实格式的设备 ID，验证码写入 DK 备注且可回读一致"
  process:
    - "探测发现未安装 → 走安装流程（下载 + /S 静默安装）"
    - "安装后 qwinsta 预检 console Active + 分辨率/窗口判定"
    - "取号前确认『允许远程协助』开关有效，取号后写 DK 备注"
accept_criteria:
  sr_threshold: 0.6
  n_min: 3
  veto: false
success_condition:
  type: all
  rules:
    - {type: tool_called, tool: occupy_device}
    - {type: llm_judge}
prep: "未安装 UU"
version: "1"
enabled: true
note: "未安装 UU 场景：探测到未装 → 先安装 → 再取号"
